# Meson build directory
build/

# wineg++ leaves tmp<hex>/ dirs in the working tree when a build fails
tmp*/

# Downloaded package files (not part of the source tree)
*.deb

# Claude Code project metadata — local to each developer's machine
.claude/

# This directory is an accidental nested checkout of the same repo.
# It should either be removed or its contents merged at the root level.
vst3bridge/

/CLAUDE.md
